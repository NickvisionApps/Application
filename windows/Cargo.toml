[package]
name = "windows"
version = "2026.7.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
windows = { version = "0.62", features = ["Storage_Pickers"] }
windows-reactor = "0.100"